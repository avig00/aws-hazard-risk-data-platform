# Legacy ETL (Deprecated)

This folder contains early script-based ETL experiments used during
initial project exploration.

The project has since been refactored to a layered architecture:

Bronze (raw ingestion)
→ Silver (Glue Spark jobs)
→ Gold (Athena-based marts)

This folder is retained for reference only and is not part of the
active data pipeline.
